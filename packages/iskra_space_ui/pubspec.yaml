name: iskra_space_ui
description: UI widgets for Iskra space (∆ Pulse, etc.)
publish_to: "none"
version: 0.0.1
environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true
