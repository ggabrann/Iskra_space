import 'package:flutter/widgets.dart';

class PulsePlaceholder extends StatelessWidget {
  const PulsePlaceholder({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
